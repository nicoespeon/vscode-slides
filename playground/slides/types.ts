export type Palette = Record<Colors, string>;

export type Colors = "magenta" | "green" | "orange";
// export type RGB = [red: number, green: number, blue: number];

export const res = {
  json: (_args: any): void => {}
};

export type Response = { hello: string };
